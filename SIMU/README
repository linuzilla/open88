Open-88 實驗模組模擬器使用說明：


一、程式：

    1.   ASM.EXE        前置組譯程式
    2.   _SIMU.LIB      模擬器的函數館


二、注意事項：

    1. ASM 是前置組譯程式，因此需要呼叫 MASM.EXE 或 TASM.EXE，請將組譯器
       放在路徑上。

    2. ASM 在處理過程中會在程式加入程式碼，因此：
       (1) 可能造成短程跳躍指令發生 Out of range 的情形。
       (2) 插入的程式碼只能在 286 以上機器執行。
       (3) 程式的畫面處理造成可觀的時間延遲。（尤其在點陣字幕顯示時）

    3. 程式目前可模擬 LED 模組、七段顯示器、揚聲器及點陣字幕顯示器。
       對鍵盤模組並未模擬。

    4. 程式忽略了程式對 8255A 工作模式的設定。


三、參數說明：

    -a  以 TASM/TLINK 組譯連結
    -s  打開 PC 喇叭模擬功能
    -p  修正七段顯示器
    -e  顯示組譯過程的錯誤訊息（會使軟碟的使用者在組譯時速度稍慢）


-----------------------------------  February 23, 1995.  Jiann-Ching Liu ------
